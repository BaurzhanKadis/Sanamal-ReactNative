import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const IpiKara = () => {
  return (
    <View style={styles.container}>
      <Text>IpiKara</Text>
    </View>
  )
}

export default IpiKara

const styles = StyleSheet.create({
    container: {
        flex: 1,
        // backgroundColor: '#fff',
        alignItems: 'center',
        justifyContent: 'center',
      },
})